import { Client } from '.pnpm/@colyseus+core@0.15.6/node_modules/@colyseus/core';
export interface Serializer<T> {
    id: string;
    reset(data: any): void;
    getFullState(client?: Client): any;
    applyPatches(clients: Client[], state: T): boolean;
    handshake?(): number[];
}
